[build]
builder = "DOCKERFILE"
watchPatterns = ["src/**", "Dockerfile", "requirements.txt"]

[deploy]
startCommand = "python src/main.py"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3

[phases.setup]
nixPkgs = ["..."] # Left blank as we use Dockerfile directly.
